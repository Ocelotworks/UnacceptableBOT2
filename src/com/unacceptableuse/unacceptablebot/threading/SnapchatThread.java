package com.unacceptableuse.unacceptablebot.threading;

import java.util.TimerTask;

public class SnapchatThread extends TimerTask {
	
    public void run() {
        //CALL VIA
    	// Timer timer = new Timer();
    	// timer.schedule(new SnapchatThread(), 0, <time in ms>);
    	
    	//DO SC GET CODE
     }

}
